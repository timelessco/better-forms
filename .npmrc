# pnpm-specific settings live in pnpm-workspace.yaml (pnpm v11 no longer
# reads minimum-release-age, public-hoist-pattern, strict-peer-dependencies,
# etc. from this file). Leaving this file in place keeps it explicit that
# we've intentionally migrated away from .npmrc-based pnpm config.
