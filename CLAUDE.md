# Reform

This project's agent instructions live in @AGENTS.md — that file is the source
of truth (stack guidance, verify commands, DB safety rules). See also
`CONTEXT.md` (glossary) and `docs/adr/` (decisions).

## Claude-specific

- Skill mappings (TanStack DB/Router skill files to load per task) live in
  `.claude/CLAUDE.md` and load automatically.
